apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev
resources:
  - serviceaccount.yaml
  - deployment.yaml
  - deployment-fechamento.yaml
  - deployment-exclusao.yaml
  - deployment-pontual.yaml
  - secret.yaml
  - triggerauthentication.yaml
  - keda-scaledobject.yaml
  - keda-scaledobject-fechamento.yaml
  - keda-scaledobject-exclusao.yaml
  - keda-scaledobject-pontual.yaml

# Define a imagem uma única vez - aplica a TODOS os 4 deployments
# Imagem local para homolog/local (não usa ECR)
images:
  - name: lambda-settlement-remuneration-unit
    newTag: dev
